/// Value, or cell, of a table.
/// The intersection of an Attribute and and ID.
class Value {
  num id;
  var value;
  String attribute;

  /// id, attribute, value.
  Value  (this.id, this.attribute, this.value);
}
